import { FormButton } from './FormButton'
import { FormInput } from './FormInput' 
import { FormWrapper } from './FormWrapper'

export const Form = {
  wrapper: FormWrapper,
  input: FormInput,
  button: FormButton
}